# ── Core Streamlit stack ──────────────────────────────────────────────────────
streamlit>=1.32.0
plotly>=5.18.0
pandas>=2.1.0

# ── Document generation ───────────────────────────────────────────────────────
reportlab>=4.0.0
python-docx>=1.1.0

# ── AI / LLM ─────────────────────────────────────────────────────────────────
groq>=0.5.0

# ── RAG stack (Option 2 — clause retrieval from standard PDFs) ────────────────
# These are only needed when you want to index and query standard PDFs locally.
# Safe to leave in requirements.txt — they are lightweight and won't break the
# app if no PDFs are uploaded.
pymupdf>=1.24.0              # PDF text extraction (fitz)
chromadb>=0.4.24             # local vector database
sentence-transformers>=2.7.0 # text embeddings (all-MiniLM-L6-v2)
